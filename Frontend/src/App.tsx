import { AppRouter } from './routes';

/**
 * App — root React component.
 * Renders the router which handles all routing and authentication.
 */
export default function App() {
  return <AppRouter />;
}
